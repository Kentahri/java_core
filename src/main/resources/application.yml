spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transactions_db?useSSL=false&serverTimezone=UTC
    username: quan
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
